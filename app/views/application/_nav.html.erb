<nav class="navbar navbar-expand-md">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <% if current_space.logo.attached? %>
        <%= image_tag current_space.logo, alt: current_space.to_s %>
      <% else %>
        <%= current_space %>
      <% end %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><%= link_to t('nav.home'), root_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to t('nav.create'), new_image_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to t('nav.images'), images_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to t('nav.about'), about_path, class: 'nav-link' %></li>
      </ul>
    </div>
  </div>
</nav>