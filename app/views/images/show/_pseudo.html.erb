<% if @ask_for_pseudo %>
  <%= simple_form_for current_user, :html=> { class: 'form' } do |f| %>
    <%= f.input :pseudo, class: 'form-control', placeholder: t('form.pseudo.placeholder')  %>
    <%= f.submit t('form.publish'), class: 'btn btn-primary mt-3 mb-3' %>
  <% end %>
<% end %>
