<%= content_for :title, 'Tableau de bord' %>

<div class="row">
  <% @spaces.each do |space| %>
    <div class="col-lg-3">
      <div class="card">
        <div class="card-body">
          <h2 class="card-title"><%= space %></h2>
          <p><%= space.images.count %> images</p>
          <i class="bi bi-arrow-right"></i>
          <%= link_to 'Accéder à l\'espace', [:admin, space], class: 'stretched-link' %>
        </div>
      </div>
    </div>
  <% end %>
</div>