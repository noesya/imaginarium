<p><%= answers.total_count %> réponses</p>
<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>Valeur</th>
        <th>Utilisateur</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
      <% answers.each do |answer| %>
        <tr>
          <td><%= answer %></td>
          <td><%= link_to answer.user, [:admin, answer.user] %></td>
          <td><%= t answer.created_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%= paginate answers, theme: 'bootstrap-5', param_name: :answers_page %>
